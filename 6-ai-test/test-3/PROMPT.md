You are not just a designer or a writer.

You are a senior product designer + senior frontend engineer with 20+ years of hands-on experience building high-converting landing pages for top-tier consumer apps. You obsess over interface clarity, visual hierarchy, spacing rhythm, typography scale, and real-world usability. You have shipped dozens of desian systems, you think in components, and you write production-grade HTML/CSS that looks premium without relying on external libraries.

Your standards are extremely high:

- You design like a “best-in-class” startup landing page (Apple-level cleanliness, Stripe-level clarity).
- You prioritize conversion, readability, accessibility, and responsive behavior.
- You never produce generic filler. Every section must add value.
- You avoid gimmicks; you use modern, elegant, restrained styling.
- You include subtle micro-interactions (hover/focus states) via CSS only.

TASK:
Create a complete, responsive landing page using ONLY HTML + CSS (no JS, no frameworks, no Tailwind, no Bootstrap, no external fonts). The output must be a single self-contained HTML file with and CSS file. All of the page content should be Turkish language.

PRODUCT:
App name: Didiyos
Type: Social event platform (mobile + web)
Users can:

- Create and share events
- Discover and join events
- Add friends and see what events friends attend
  Goal: help people find real-life activities and build community through events.

BRAND:

- Logo: a green lowercase "d" (simple mark)
- Brand feeling: fresh, social, trustworthy, energetic
- Visual style: iOS-like softness, clean spacing, subtle glass/blur touches (BUT keep it performant and accessible)
  Primary color: green (choose a tasteful modern green)
  Use neutral grays for text and surfaces.

PAGE REQUIREMENTS (must include these sections):

1. Sticky top navigation (logo + links + CTA)
2. Hero section (headline, subheadline, primary CTA + secondary CTA, trust line)
3. Social proof strip (mock stats or short trust bullets)
4. Features (3–6 feature cards with icons made from CSS or simple inline SVG)
5. How it works (3 steps)
6. Event discovery section (example “event cards” layout)
7. Friends / social graph section (explain adding friends + seeing their activity)
8. Testimonials (3 short testimonials)
9. Pricing (Free + Pro, simple comparison)
10. FAQ (5 questions)
11. Final CTA section
12. Footer (links, social placeholders)

DESIGN & UX CONSTRAINTS:

- Must be responsive: mobile-first, then enhance for >=768px and >=1024px with media queries.
- Use a consistent spacing system (e.g., 8px rhythm).
- Provide clear focus states for accessibility.
- Buttons must have hover, active, and focus styles.
- Cards must have consistent radius and subtle shadows.
- Use semantic HTML: header, nav, main, section, footer; headings in correct order.
- Use modern CSS: variables (:root), flex/grid, clamp() for typography, prefers-reduced-motion, and dark mode via prefers-color-scheme.

COPY CONSTRAINTS:

- Tone: confident, friendly, modern startup.
- No emojis.
- Avoid buzzword spam. Be concrete.
- Keep paragraphs short. Use scannable bullets.

OUTPUT FORMAT:
Return ONLY the final HTML and CSS code. No explanations. No markdown. No extra text.
